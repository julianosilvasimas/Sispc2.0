export interface CadIndicador{
    indicador_id: number, 
    indicador: string, 
    gerencia: number, 
    classificacao : number, 
    tipo_grafico : number,
    ordem : number,
    tendencia : number
}
